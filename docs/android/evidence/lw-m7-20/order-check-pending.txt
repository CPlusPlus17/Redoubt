error: UNCLASSIFIED shared-file pair - a human has to look at this:
      patches/android/canvas-webgl-permissions.patch   (android.txt)
      patches/android/sync-opt-in.patch   (android.txt)
    co-applied on: android
    shared files:
      mobile/android/fenix/app/src/main/java/org/mozilla/fenix/browser/BaseBrowserFragment.kt: hunks are in disjoint regions
    The overlap note is a hint, not a verdict. On this tree it flags 1 of the 5
    known constraints and 6 pairs that are fine, and hunk headers of a later
    patch are in post-earlier-patch coordinates anyway. Read the patches.
    Two patches editing the same tree file must be ordered, not
    parallel (docs/android/AGENTS.md, "Ownership"). Decide which
    it is and record it in scripts/check-patch-order.py:
      - one order required -> add to CONSTRAINTS:
          ('patches/android/canvas-webgl-permissions.patch',
           'patches/android/sync-opt-in.patch',
           'mobile/android/fenix/app/src/main/java/org/mozilla/fenix/browser/BaseBrowserFragment.kt',
           "<evidence>"),
      - either order fine   -> add to REVIEWED_ORDER_FREE:
          ('patches/android/canvas-webgl-permissions.patch',
           'patches/android/sync-opt-in.patch',
           ('mobile/android/fenix/app/src/main/java/org/mozilla/fenix/browser/BaseBrowserFragment.kt',),
           "<reason>"),
error: UNCLASSIFIED shared-file pair - a human has to look at this:
      patches/android/no-adjust.patch   (android.txt)
      patches/android/sync-opt-in.patch   (android.txt)
    co-applied on: android
    shared files:
      mobile/android/fenix/app/src/main/java/org/mozilla/fenix/FenixApplication.kt: hunks are in disjoint regions
      mobile/android/fenix/app/src/main/java/org/mozilla/fenix/HomeActivity.kt: hunks overlap or abut
    The overlap note is a hint, not a verdict. On this tree it flags 1 of the 5
    known constraints and 6 pairs that are fine, and hunk headers of a later
    patch are in post-earlier-patch coordinates anyway. Read the patches.
    Two patches editing the same tree file must be ordered, not
    parallel (docs/android/AGENTS.md, "Ownership"). Decide which
    it is and record it in scripts/check-patch-order.py:
      - one order required -> add to CONSTRAINTS:
          ('patches/android/no-adjust.patch',
           'patches/android/sync-opt-in.patch',
           'mobile/android/fenix/app/src/main/java/org/mozilla/fenix/FenixApplication.kt',
           "<evidence>"),
      - either order fine   -> add to REVIEWED_ORDER_FREE:
          ('patches/android/no-adjust.patch',
           'patches/android/sync-opt-in.patch',
           ('mobile/android/fenix/app/src/main/java/org/mozilla/fenix/FenixApplication.kt', 'mobile/android/fenix/app/src/main/java/org/mozilla/fenix/HomeActivity.kt'),
           "<reason>"),
error: UNCLASSIFIED shared-file pair - a human has to look at this:
      patches/android/no-glean.patch   (android.txt)
      patches/android/sync-opt-in.patch   (android.txt)
    co-applied on: android
    shared files:
      mobile/android/fenix/app/src/main/java/org/mozilla/fenix/FenixApplication.kt: hunks are in disjoint regions
    The overlap note is a hint, not a verdict. On this tree it flags 1 of the 5
    known constraints and 6 pairs that are fine, and hunk headers of a later
    patch are in post-earlier-patch coordinates anyway. Read the patches.
    Two patches editing the same tree file must be ordered, not
    parallel (docs/android/AGENTS.md, "Ownership"). Decide which
    it is and record it in scripts/check-patch-order.py:
      - one order required -> add to CONSTRAINTS:
          ('patches/android/no-glean.patch',
           'patches/android/sync-opt-in.patch',
           'mobile/android/fenix/app/src/main/java/org/mozilla/fenix/FenixApplication.kt',
           "<evidence>"),
      - either order fine   -> add to REVIEWED_ORDER_FREE:
          ('patches/android/no-glean.patch',
           'patches/android/sync-opt-in.patch',
           ('mobile/android/fenix/app/src/main/java/org/mozilla/fenix/FenixApplication.kt',),
           "<reason>"),
error: UNCLASSIFIED shared-file pair - a human has to look at this:
      patches/android/no-gms.patch   (android.txt)
      patches/android/sync-opt-in.patch   (android.txt)
    co-applied on: android
    shared files:
      mobile/android/fenix/app/src/main/java/org/mozilla/fenix/FenixApplication.kt: hunks overlap or abut
      mobile/android/fenix/app/src/main/java/org/mozilla/fenix/components/BackgroundServices.kt: hunks overlap or abut
    The overlap note is a hint, not a verdict. On this tree it flags 1 of the 5
    known constraints and 6 pairs that are fine, and hunk headers of a later
    patch are in post-earlier-patch coordinates anyway. Read the patches.
    Two patches editing the same tree file must be ordered, not
    parallel (docs/android/AGENTS.md, "Ownership"). Decide which
    it is and record it in scripts/check-patch-order.py:
      - one order required -> add to CONSTRAINTS:
          ('patches/android/no-gms.patch',
           'patches/android/sync-opt-in.patch',
           'mobile/android/fenix/app/src/main/java/org/mozilla/fenix/FenixApplication.kt',
           "<evidence>"),
      - either order fine   -> add to REVIEWED_ORDER_FREE:
          ('patches/android/no-gms.patch',
           'patches/android/sync-opt-in.patch',
           ('mobile/android/fenix/app/src/main/java/org/mozilla/fenix/FenixApplication.kt', 'mobile/android/fenix/app/src/main/java/org/mozilla/fenix/components/BackgroundServices.kt'),
           "<reason>"),
error: UNCLASSIFIED shared-file pair - a human has to look at this:
      patches/android/no-nimbus.patch   (android.txt)
      patches/android/sync-opt-in.patch   (android.txt)
    co-applied on: android
    shared files:
      mobile/android/fenix/app/src/main/java/org/mozilla/fenix/HomeActivity.kt: hunks overlap or abut
    The overlap note is a hint, not a verdict. On this tree it flags 1 of the 5
    known constraints and 6 pairs that are fine, and hunk headers of a later
    patch are in post-earlier-patch coordinates anyway. Read the patches.
    Two patches editing the same tree file must be ordered, not
    parallel (docs/android/AGENTS.md, "Ownership"). Decide which
    it is and record it in scripts/check-patch-order.py:
      - one order required -> add to CONSTRAINTS:
          ('patches/android/no-nimbus.patch',
           'patches/android/sync-opt-in.patch',
           'mobile/android/fenix/app/src/main/java/org/mozilla/fenix/HomeActivity.kt',
           "<evidence>"),
      - either order fine   -> add to REVIEWED_ORDER_FREE:
          ('patches/android/no-nimbus.patch',
           'patches/android/sync-opt-in.patch',
           ('mobile/android/fenix/app/src/main/java/org/mozilla/fenix/HomeActivity.kt',),
           "<reason>"),
error: UNCLASSIFIED shared-file pair - a human has to look at this:
      patches/android/no-suggest.patch   (android.txt)
      patches/android/sync-opt-in.patch   (android.txt)
    co-applied on: android
    shared files:
      mobile/android/fenix/app/src/main/java/org/mozilla/fenix/HomeActivity.kt: hunks overlap or abut
    The overlap note is a hint, not a verdict. On this tree it flags 1 of the 5
    known constraints and 6 pairs that are fine, and hunk headers of a later
    patch are in post-earlier-patch coordinates anyway. Read the patches.
    Two patches editing the same tree file must be ordered, not
    parallel (docs/android/AGENTS.md, "Ownership"). Decide which
    it is and record it in scripts/check-patch-order.py:
      - one order required -> add to CONSTRAINTS:
          ('patches/android/no-suggest.patch',
           'patches/android/sync-opt-in.patch',
           'mobile/android/fenix/app/src/main/java/org/mozilla/fenix/HomeActivity.kt',
           "<evidence>"),
      - either order fine   -> add to REVIEWED_ORDER_FREE:
          ('patches/android/no-suggest.patch',
           'patches/android/sync-opt-in.patch',
           ('mobile/android/fenix/app/src/main/java/org/mozilla/fenix/HomeActivity.kt',),
           "<reason>"),
error: UNCLASSIFIED shared-file pair - a human has to look at this:
      patches/android/ubo-preinstall.patch   (android.txt)
      patches/android/sync-opt-in.patch   (android.txt)
    co-applied on: android
    shared files:
      mobile/android/fenix/app/src/main/java/org/mozilla/fenix/FenixApplication.kt: hunks are in disjoint regions
      mobile/android/fenix/app/src/main/java/org/mozilla/fenix/HomeActivity.kt: hunks overlap or abut
    The overlap note is a hint, not a verdict. On this tree it flags 1 of the 5
    known constraints and 6 pairs that are fine, and hunk headers of a later
    patch are in post-earlier-patch coordinates anyway. Read the patches.
    Two patches editing the same tree file must be ordered, not
    parallel (docs/android/AGENTS.md, "Ownership"). Decide which
    it is and record it in scripts/check-patch-order.py:
      - one order required -> add to CONSTRAINTS:
          ('patches/android/ubo-preinstall.patch',
           'patches/android/sync-opt-in.patch',
           'mobile/android/fenix/app/src/main/java/org/mozilla/fenix/FenixApplication.kt',
           "<evidence>"),
      - either order fine   -> add to REVIEWED_ORDER_FREE:
          ('patches/android/ubo-preinstall.patch',
           'patches/android/sync-opt-in.patch',
           ('mobile/android/fenix/app/src/main/java/org/mozilla/fenix/FenixApplication.kt', 'mobile/android/fenix/app/src/main/java/org/mozilla/fenix/HomeActivity.kt'),
           "<reason>"),
error: UNCLASSIFIED shared-file pair - a human has to look at this:
      patches/android/update-check.patch   (android.txt)
      patches/android/sync-opt-in.patch   (android.txt)
    co-applied on: android
    shared files:
      mobile/android/fenix/app/src/main/java/org/mozilla/fenix/HomeActivity.kt: hunks are in disjoint regions
      mobile/android/fenix/app/src/main/java/org/mozilla/fenix/settings/SettingsFragment.kt: hunks are in disjoint regions
    The overlap note is a hint, not a verdict. On this tree it flags 1 of the 5
    known constraints and 6 pairs that are fine, and hunk headers of a later
    patch are in post-earlier-patch coordinates anyway. Read the patches.
    Two patches editing the same tree file must be ordered, not
    parallel (docs/android/AGENTS.md, "Ownership"). Decide which
    it is and record it in scripts/check-patch-order.py:
      - one order required -> add to CONSTRAINTS:
          ('patches/android/update-check.patch',
           'patches/android/sync-opt-in.patch',
           'mobile/android/fenix/app/src/main/java/org/mozilla/fenix/HomeActivity.kt',
           "<evidence>"),
      - either order fine   -> add to REVIEWED_ORDER_FREE:
          ('patches/android/update-check.patch',
           'patches/android/sync-opt-in.patch',
           ('mobile/android/fenix/app/src/main/java/org/mozilla/fenix/HomeActivity.kt', 'mobile/android/fenix/app/src/main/java/org/mozilla/fenix/settings/SettingsFragment.kt'),
           "<reason>"),

8 problem(s). See "Patch ordering constraints" in docs/android/AGENTS.md.
