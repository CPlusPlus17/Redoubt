# Process initialization history followup

This source audit retains the preceding eight-file audit snapshot in before-review.tar.gz. It updates only five existing current repository bindings:20,26 and their29/35/36 receipts. The original46-file source capture, all59 semantic repository input slots, all counterpart records, desktop effects, policy leaves and pane controls remain unchanged.

The checker first verifies the earlier Bundle114→fixtureA8→OptInB10, Home9f→GNU28→navigationAC0 and Suggest metrics86 stages from their exact archives. It then checks the new20/26 before/after source receipts and actual combined167 overlay, including161 unaffected source hashes and three added authored cases. The six-file overlay is different from the intermediate scoped20 outputs; only the combined20+26 source bodies represent corrected167.

Read source: both parent policy initializers now run under the existing main-process guard before superclass attachment. The child installs a constant four-false Suggest reader and closes AccountServices in memory, invalidating admission generations and preventing restart-choice persistence in that process. It reads no policy preferences. The legacy test helper explicitly opts in after superclass attachment; the new tests attach plain production Application instances. Saved parent choices and other A-C embedder defaults are preserved. The mocked process helper in the unit tests does not demonstrate the Android process-name detector. API28+ uses Application.getProcessName; the upstream API26/27 fallback queries a nullable ActivityManager before its isolation short-circuit.

Verification: `python3 docs/android/evidence/lw-m7-17/check-coverage.py`, `python3 docs/android/evidence/lw-m7-17/global-controls-followup/test-checker.py`, `python3 docs/android/evidence/lw-m7-17/fixture-lineage-followup/test-checker.py`, and `python3 docs/android/board.py --check`. The original15 negative controls remain; four added controls reject a false process runtime claim, a collapsed parent history, a wrong actual before hash and an invented extra regression. No target code runs here. Corrected167 diagnostic/full-suite/APK/runtime receipts and future35/36 native acceptance are separate.
