$ python3 scripts/check-patch-order.py
error: UNCLASSIFIED shared-file pair - a human has to look at this:
      patches/android/canvas-webgl-permissions.patch   (android.txt)
      patches/android/cookie-banner-controls.patch   (android.txt)
    co-applied on: android
    shared files:
      mobile/android/fenix/app/src/main/java/org/mozilla/fenix/settings/quicksettings/QuickSettingsSheetDialogFragment.kt: hunks are in disjoint regions
      mobile/android/fenix/app/src/main/java/org/mozilla/fenix/settings/trustpanel/TrustPanelFragment.kt: hunks overlap or abut
      mobile/android/fenix/app/src/main/java/org/mozilla/fenix/settings/trustpanel/ui/ProtectionPanel.kt: hunks overlap or abut
      mobile/android/fenix/app/src/main/res/values/strings.xml: hunks are in disjoint regions
      mobile/shared/modules/geckoview/GeckoViewStorageController.sys.mjs: hunks are in disjoint regions
    The overlap note is a hint, not a verdict. On this tree it flags 1 of the 5
    known constraints and 6 pairs that are fine, and hunk headers of a later
    patch are in post-earlier-patch coordinates anyway. Read the patches.
    Two patches editing the same tree file must be ordered, not
    parallel (docs/android/AGENTS.md, "Ownership"). Decide which
    it is and record it in scripts/check-patch-order.py:
      - one order required -> add to CONSTRAINTS:
          ('patches/android/canvas-webgl-permissions.patch',
           'patches/android/cookie-banner-controls.patch',
           'mobile/android/fenix/app/src/main/java/org/mozilla/fenix/settings/quicksettings/QuickSettingsSheetDialogFragment.kt',
           "<evidence>"),
      - either order fine   -> add to REVIEWED_ORDER_FREE:
          ('patches/android/canvas-webgl-permissions.patch',
           'patches/android/cookie-banner-controls.patch',
           ('mobile/android/fenix/app/src/main/java/org/mozilla/fenix/settings/quicksettings/QuickSettingsSheetDialogFragment.kt', 'mobile/android/fenix/app/src/main/java/org/mozilla/fenix/settings/trustpanel/TrustPanelFragment.kt', 'mobile/android/fenix/app/src/main/java/org/mozilla/fenix/settings/trustpanel/ui/ProtectionPanel.kt', 'mobile/android/fenix/app/src/main/res/values/strings.xml', 'mobile/shared/modules/geckoview/GeckoViewStorageController.sys.mjs'),
           "<reason>"),
error: UNCLASSIFIED shared-file pair - a human has to look at this:
      patches/android/no-adjust.patch   (android.txt)
      patches/android/cookie-banner-controls.patch   (android.txt)
    co-applied on: android
    shared files:
      mobile/android/fenix/app/src/main/java/org/mozilla/fenix/utils/Settings.kt: hunks are in disjoint regions
    The overlap note is a hint, not a verdict. On this tree it flags 1 of the 5
    known constraints and 6 pairs that are fine, and hunk headers of a later
    patch are in post-earlier-patch coordinates anyway. Read the patches.
    Two patches editing the same tree file must be ordered, not
    parallel (docs/android/AGENTS.md, "Ownership"). Decide which
    it is and record it in scripts/check-patch-order.py:
      - one order required -> add to CONSTRAINTS:
          ('patches/android/no-adjust.patch',
           'patches/android/cookie-banner-controls.patch',
           'mobile/android/fenix/app/src/main/java/org/mozilla/fenix/utils/Settings.kt',
           "<evidence>"),
      - either order fine   -> add to REVIEWED_ORDER_FREE:
          ('patches/android/no-adjust.patch',
           'patches/android/cookie-banner-controls.patch',
           ('mobile/android/fenix/app/src/main/java/org/mozilla/fenix/utils/Settings.kt',),
           "<reason>"),
error: UNCLASSIFIED shared-file pair - a human has to look at this:
      patches/android/no-crashreporter.patch   (android.txt)
      patches/android/cookie-banner-controls.patch   (android.txt)
    co-applied on: android
    shared files:
      mobile/android/fenix/app/src/main/res/values/strings.xml: hunks are in disjoint regions
    The overlap note is a hint, not a verdict. On this tree it flags 1 of the 5
    known constraints and 6 pairs that are fine, and hunk headers of a later
    patch are in post-earlier-patch coordinates anyway. Read the patches.
    Two patches editing the same tree file must be ordered, not
    parallel (docs/android/AGENTS.md, "Ownership"). Decide which
    it is and record it in scripts/check-patch-order.py:
      - one order required -> add to CONSTRAINTS:
          ('patches/android/no-crashreporter.patch',
           'patches/android/cookie-banner-controls.patch',
           'mobile/android/fenix/app/src/main/res/values/strings.xml',
           "<evidence>"),
      - either order fine   -> add to REVIEWED_ORDER_FREE:
          ('patches/android/no-crashreporter.patch',
           'patches/android/cookie-banner-controls.patch',
           ('mobile/android/fenix/app/src/main/res/values/strings.xml',),
           "<reason>"),
error: UNCLASSIFIED shared-file pair - a human has to look at this:
      patches/android/no-gms.patch   (android.txt)
      patches/android/cookie-banner-controls.patch   (android.txt)
    co-applied on: android
    shared files:
      mobile/android/fenix/app/src/main/java/org/mozilla/fenix/utils/Settings.kt: hunks are in disjoint regions
      mobile/android/fenix/app/src/main/res/values/preference_keys.xml: hunks are in disjoint regions
    The overlap note is a hint, not a verdict. On this tree it flags 1 of the 5
    known constraints and 6 pairs that are fine, and hunk headers of a later
    patch are in post-earlier-patch coordinates anyway. Read the patches.
    Two patches editing the same tree file must be ordered, not
    parallel (docs/android/AGENTS.md, "Ownership"). Decide which
    it is and record it in scripts/check-patch-order.py:
      - one order required -> add to CONSTRAINTS:
          ('patches/android/no-gms.patch',
           'patches/android/cookie-banner-controls.patch',
           'mobile/android/fenix/app/src/main/java/org/mozilla/fenix/utils/Settings.kt',
           "<evidence>"),
      - either order fine   -> add to REVIEWED_ORDER_FREE:
          ('patches/android/no-gms.patch',
           'patches/android/cookie-banner-controls.patch',
           ('mobile/android/fenix/app/src/main/java/org/mozilla/fenix/utils/Settings.kt', 'mobile/android/fenix/app/src/main/res/values/preference_keys.xml'),
           "<reason>"),
error: UNCLASSIFIED shared-file pair - a human has to look at this:
      patches/android/no-onboarding.patch   (android.txt)
      patches/android/cookie-banner-controls.patch   (android.txt)
    co-applied on: android
    shared files:
      mobile/android/fenix/app/src/main/java/org/mozilla/fenix/utils/Settings.kt: hunks are in disjoint regions
    The overlap note is a hint, not a verdict. On this tree it flags 1 of the 5
    known constraints and 6 pairs that are fine, and hunk headers of a later
    patch are in post-earlier-patch coordinates anyway. Read the patches.
    Two patches editing the same tree file must be ordered, not
    parallel (docs/android/AGENTS.md, "Ownership"). Decide which
    it is and record it in scripts/check-patch-order.py:
      - one order required -> add to CONSTRAINTS:
          ('patches/android/no-onboarding.patch',
           'patches/android/cookie-banner-controls.patch',
           'mobile/android/fenix/app/src/main/java/org/mozilla/fenix/utils/Settings.kt',
           "<evidence>"),
      - either order fine   -> add to REVIEWED_ORDER_FREE:
          ('patches/android/no-onboarding.patch',
           'patches/android/cookie-banner-controls.patch',
           ('mobile/android/fenix/app/src/main/java/org/mozilla/fenix/utils/Settings.kt',),
           "<reason>"),
error: UNCLASSIFIED shared-file pair - a human has to look at this:
      patches/android/no-suggest.patch   (android.txt)
      patches/android/cookie-banner-controls.patch   (android.txt)
    co-applied on: android
    shared files:
      mobile/android/fenix/app/src/main/java/org/mozilla/fenix/utils/Settings.kt: hunks are in disjoint regions
    The overlap note is a hint, not a verdict. On this tree it flags 1 of the 5
    known constraints and 6 pairs that are fine, and hunk headers of a later
    patch are in post-earlier-patch coordinates anyway. Read the patches.
    Two patches editing the same tree file must be ordered, not
    parallel (docs/android/AGENTS.md, "Ownership"). Decide which
    it is and record it in scripts/check-patch-order.py:
      - one order required -> add to CONSTRAINTS:
          ('patches/android/no-suggest.patch',
           'patches/android/cookie-banner-controls.patch',
           'mobile/android/fenix/app/src/main/java/org/mozilla/fenix/utils/Settings.kt',
           "<evidence>"),
      - either order fine   -> add to REVIEWED_ORDER_FREE:
          ('patches/android/no-suggest.patch',
           'patches/android/cookie-banner-controls.patch',
           ('mobile/android/fenix/app/src/main/java/org/mozilla/fenix/utils/Settings.kt',),
           "<reason>"),
error: UNCLASSIFIED shared-file pair - a human has to look at this:
      patches/android/privacy-defaults.patch   (android.txt)
      patches/android/cookie-banner-controls.patch   (android.txt)
    co-applied on: android
    shared files:
      mobile/android/fenix/app/src/main/java/org/mozilla/fenix/utils/Settings.kt: hunks are in disjoint regions
    The overlap note is a hint, not a verdict. On this tree it flags 1 of the 5
    known constraints and 6 pairs that are fine, and hunk headers of a later
    patch are in post-earlier-patch coordinates anyway. Read the patches.
    Two patches editing the same tree file must be ordered, not
    parallel (docs/android/AGENTS.md, "Ownership"). Decide which
    it is and record it in scripts/check-patch-order.py:
      - one order required -> add to CONSTRAINTS:
          ('patches/android/privacy-defaults.patch',
           'patches/android/cookie-banner-controls.patch',
           'mobile/android/fenix/app/src/main/java/org/mozilla/fenix/utils/Settings.kt',
           "<evidence>"),
      - either order fine   -> add to REVIEWED_ORDER_FREE:
          ('patches/android/privacy-defaults.patch',
           'patches/android/cookie-banner-controls.patch',
           ('mobile/android/fenix/app/src/main/java/org/mozilla/fenix/utils/Settings.kt',),
           "<reason>"),
error: UNCLASSIFIED shared-file pair - a human has to look at this:
      patches/android/search-config.patch   (android.txt)
      patches/android/cookie-banner-controls.patch   (android.txt)
    co-applied on: android
    shared files:
      mobile/android/fenix/app/src/main/java/org/mozilla/fenix/utils/Settings.kt: hunks are in disjoint regions
    The overlap note is a hint, not a verdict. On this tree it flags 1 of the 5
    known constraints and 6 pairs that are fine, and hunk headers of a later
    patch are in post-earlier-patch coordinates anyway. Read the patches.
    Two patches editing the same tree file must be ordered, not
    parallel (docs/android/AGENTS.md, "Ownership"). Decide which
    it is and record it in scripts/check-patch-order.py:
      - one order required -> add to CONSTRAINTS:
          ('patches/android/search-config.patch',
           'patches/android/cookie-banner-controls.patch',
           'mobile/android/fenix/app/src/main/java/org/mozilla/fenix/utils/Settings.kt',
           "<evidence>"),
      - either order fine   -> add to REVIEWED_ORDER_FREE:
          ('patches/android/search-config.patch',
           'patches/android/cookie-banner-controls.patch',
           ('mobile/android/fenix/app/src/main/java/org/mozilla/fenix/utils/Settings.kt',),
           "<reason>"),
error: UNCLASSIFIED shared-file pair - a human has to look at this:
      patches/android/ubo-preinstall.patch   (android.txt)
      patches/android/cookie-banner-controls.patch   (android.txt)
    co-applied on: android
    shared files:
      mobile/android/fenix/app/src/main/res/values/strings.xml: hunks are in disjoint regions
    The overlap note is a hint, not a verdict. On this tree it flags 1 of the 5
    known constraints and 6 pairs that are fine, and hunk headers of a later
    patch are in post-earlier-patch coordinates anyway. Read the patches.
    Two patches editing the same tree file must be ordered, not
    parallel (docs/android/AGENTS.md, "Ownership"). Decide which
    it is and record it in scripts/check-patch-order.py:
      - one order required -> add to CONSTRAINTS:
          ('patches/android/ubo-preinstall.patch',
           'patches/android/cookie-banner-controls.patch',
           'mobile/android/fenix/app/src/main/res/values/strings.xml',
           "<evidence>"),
      - either order fine   -> add to REVIEWED_ORDER_FREE:
          ('patches/android/ubo-preinstall.patch',
           'patches/android/cookie-banner-controls.patch',
           ('mobile/android/fenix/app/src/main/res/values/strings.xml',),
           "<reason>"),
error: UNCLASSIFIED shared-file pair - a human has to look at this:
      patches/android/update-check.patch   (android.txt)
      patches/android/cookie-banner-controls.patch   (android.txt)
    co-applied on: android
    shared files:
      mobile/android/fenix/app/src/main/java/org/mozilla/fenix/settings/SettingsFragment.kt: hunks are in disjoint regions
      mobile/android/fenix/app/src/main/res/xml/preferences.xml: hunks are in disjoint regions
    The overlap note is a hint, not a verdict. On this tree it flags 1 of the 5
    known constraints and 6 pairs that are fine, and hunk headers of a later
    patch are in post-earlier-patch coordinates anyway. Read the patches.
    Two patches editing the same tree file must be ordered, not
    parallel (docs/android/AGENTS.md, "Ownership"). Decide which
    it is and record it in scripts/check-patch-order.py:
      - one order required -> add to CONSTRAINTS:
          ('patches/android/update-check.patch',
           'patches/android/cookie-banner-controls.patch',
           'mobile/android/fenix/app/src/main/java/org/mozilla/fenix/settings/SettingsFragment.kt',
           "<evidence>"),
      - either order fine   -> add to REVIEWED_ORDER_FREE:
          ('patches/android/update-check.patch',
           'patches/android/cookie-banner-controls.patch',
           ('mobile/android/fenix/app/src/main/java/org/mozilla/fenix/settings/SettingsFragment.kt', 'mobile/android/fenix/app/src/main/res/xml/preferences.xml'),
           "<reason>"),

10 problem(s). See "Patch ordering constraints" in docs/android/AGENTS.md.

exit=1
