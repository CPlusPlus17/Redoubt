# APK compilation passed; resource verification stopped the checkpoint

Root captured invocation `202a41bedcc148ef9faf7d2180eec463` read-only after
termination. Gradle reported `BUILD SUCCESSFUL` and mach exit0 at
2026-09-09T03:21:56Z. The four development APK hashes and sizes are retained in
`result.json`. This candidate uses native4 and the resource30 plus four Kotlin
source corrections; all165 bindings matched before and after compilation and
again before and after capture. The manifest is
`c53736e87152ba6de7ae232efd0a26197601027e4553be3f5f4bd7e6045601ae`.

The following Task30 check raised `KeyError` while opening the literal ZIP path
`res/raw/initial_shortcuts.json`. The parent checkpoint exited1 before unit tests
or runtime; the APK driver did not write its `finished.txt`. This record does not
establish packaged shortcut behavior or APK acceptance. The full compiler log,
failed checker output, Android resource table and ZIP entries remain archived for
diagnosis. Root independently checked all archive members and both165 source
check logs; `root-verification.json` binds that inspection.

The release signing key was not used. Development APK files remain in the
isolated guest; the repository retains their measured identities and build
evidence, not their full binary contents.
