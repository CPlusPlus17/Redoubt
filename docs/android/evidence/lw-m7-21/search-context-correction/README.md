# Attached Fenix settings context

The three card-selector expressions were outside a Preference.apply receiver;
their context resolved to nullable Fragment.context. They run in onResume after
super.onResume, so they now explicitly use requireContext(). Preference receiver
accesses elsewhere remain unchanged. No choice defaults or admission behavior
change. Source overlay records the actual failed before hash and fixed file.

Root replayed the20 Task26 files and all17 Task29 code/13 generated files. The
unchanged29 patch applies to the corrected shared SearchEngineFragment; its
output differs only in these same three expressions. Original26/29 receipts
remain here. These source checks do not prove target compilation.

The separately named stage/driver/checkpoint retain both earlier failures and
successful native4 evidence. Only this Fenix Kotlin file is changed, atop the
prior two Android Components corrections. APK retry and full tests/runtime are
pending. The native AARs remain independently hash-verified.
