# Post-native4 composition with permission Bundle correction

Local composition by `/root/coverage_map`; no guest/source staging, compiler,
target tests or device execution. Inputs are pinned at `35062553de8f6d2c9c6d2e6e87e372ca94d6d37e`,
the one-line Task36 dependency-receipt refresh on root implementation `301e662`.
The prior `bc27033` handoff remains unchanged in its separate artifact directory.

This audit reconstructs the retained native4+30 manifest, verifies all three
subsequent failed APK archives/165-file manifests and four Kotlin overlays, then
replays31→29→corrected35→36 in registry order. It independently reconstructs each
task's exact before state from archived source and scoped predecessors, verifies
all final task pins, and rejects any later task whose expected before state does
not match the actual preceding output. GNU patch dry-run/apply uses `--fuzz=0`;
all composed patches have zero offsets. The current14 patch also independently
reconstructs the corrected permission-dialog bytes. Task36's historical original
audit sources are untouched; only its current scoped14 dependency digest changed.

The final union still has230 bindings. The materialized subset now has79 files:
43 creations,32 replacements and4 retained bound files (two admission fixes,
the permission dialog, and the empty30 shortcut resource). It includes all13
Suggest catalog/config/test-fixture resources generated by the pinned actual
packager, without downloads. All78 previously materialized bodies are unchanged;
the one new body replaces the old permission-dialog binding. Consequently only
one of230 full manifest rows changes, as `revision-comparison.json` verifies.

Final230 manifest SHA256:
`77a62ed53e5aa026e8acec4a995f6ab7d8ee918cc0ac17cea6cef62c934341d5`.
Source-subset archive SHA256:
`dc48c272488aa3e0623925eca82fdd47cae981c707a250f71868ca5df6cd3389`.
Task36 current source-files.json SHA256:
`53af70a5559a513e2ead11e2f511a09ab41eafa06d8c19f3472d1f6b36fcc04b`.

Before staging, verify the corrected165 manifest plus22 existing before hashes
and43 required absences outside that manifest. Stage all replacements/creations
only after that preflight, then bind/check the entire230 union before and after
the future build. The151 untouched native4 source bodies remain original compiled
manifest bindings; this sparse archive does not pretend to contain or reconstruct
them. Live hashes must match. Preserve original native4 and all failed APK source
receipts, using new namespaces for new staging/build results.

Task27 independently retains four additional unchanged native-test source pins:
Extension.sys.mjs, ExtensionTaskScheduler.sys.mjs, test_ext_permissions.js and
test_ext_permissions_uninstall.js. Its owner should combine that reviewed
supplement with the updated230 bindings for the234 native-test preflight. Do not
drop those requirements or treat this source union as body evidence for them.

The script and105 input-file pins are reproducible with a read-only snapshot of
the recorded repository inputs. An input repository with altered files fails.
Output must be a new private directory outside that repository:

```sh
python3 audit.py --repo /path/to/pinned-inputs --output /new/private/result
```

`receipt.json` binds script, inputs and artifacts. Two complete local runs produced
identical receipts/output hashes. `proposed-staging.json` is a plan, not a staging
receipt. No composition result establishes target compilation, tests or runtime
success for these new changes.
