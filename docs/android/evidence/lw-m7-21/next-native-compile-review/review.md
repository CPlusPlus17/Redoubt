No concrete compile or API integration blocker found in the bounded Task37 review against corrected245 `40da1bf9c42187b1e037fba5b443e4b26fd2758ffa7b07212710df72693eb806`. Actual native compilation and GeckoView execution remain pending. No patches, current pins or guest sources were changed.

Evidence checked:

- `netwerk/cookie/nsICookieManager.idl`: ran the frozen XPIDL parser/resolver and C++ declaration generator on the actual candidate IDL. All four generated method signatures match the CookieService implementations, including `const nsTArray<uint32_t>&`, the implicit JSContext and DOM Promise output. CookieService is the only native nsICookieManager implementation found.
- `CookieService.cpp`, `CookiePersistentStorage.{h,cpp}`, `CookieStorage.{h,cpp}`: includes, public/protected access and static/virtual declarations match. Actual mozStorage callback argument types, async statement/binding methods, UUID factory and JS object/array conversion overloads exist in the inspected declarations. OriginAttributes derives from the supported WebIDL dictionary type.
- `dom/base/nsFrameLoader.{h,cpp}` and `dom/chrome-webidl/FrameLoader.webidl`: the native name, Promise/ErrorResult signature, complete Promise include, constructor field order and cycle collection member match the existing binding patterns. The selected privileged global is the same global used by existing GetParentObject.
- `netwerk/cookie/moz.build`, `dom/chrome-webidl/moz.build`, `dom/bindings/Bindings.conf`: modified cookie sources/IDL and FrameLoader WebIDL are already registered; the test manifest includes the added test. No newly added production translation unit needs registration.
- `SessionCleanupTest.kt`, test-support JS/schema and existing BaseSessionTest/GeckoSessionTestRule/Environment: called Kotlin helper methods, environment properties, crash annotation and three test-extension signatures exist. The crash test retains its explicit unsupported-process assumptions; no execution or accepted skip is inferred from this read.

`idl-result.json` and `nsICookieManager.generated.h` retain the host-only generator result. `review-inputs.json`/`review-inputs.tar.gz` bind all15 candidate bodies and inspected frozen declarations. This generator check does not establish C++ linking, Android Kotlin compilation, native cookie behavior, frame lifetimes or the unfinished full-session coordinator.
